services:
  chronicle-pdf:
    build:
      context: ../..
      dockerfile: docker/embedded/Dockerfile.fat
    container_name: Chronicle-PDF-fat
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 6G
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080$${SYSTEM_ROOTURIPATH:-''}/api/v1/info/status | grep -q 'UP'"]
      interval: 5s
      timeout: 10s
      retries: 16
    ports:
      - "8080:8080"
    volumes:
      - ../../chronicle/latest/data:/usr/share/tessdata:rw
      - ../../chronicle/latest/config:/configs:rw
      - ../../chronicle/latest/logs:/logs:rw
    environment:
      DISABLE_ADDITIONAL_FEATURES: "false"
      SECURITY_ENABLELOGIN: "false"
      FAT_DOCKER: "true"
      SYSTEM_DEFAULTLOCALE: en-US
      UI_APPNAME: Chronicle PDF
      UI_HOMEDESCRIPTION: Full-featured Chronicle PDF with all capabilities
      UI_APPNAMENAVBAR: Chronicle PDF Fat
      SYSTEM_MAXFILESIZE: "200"
      METRICS_ENABLED: "true"
      SYSTEM_GOOGLEVISIBILITY: "true"
      SHOW_SURVEY: "true"
    networks:
      - chronicle-network

networks:
  chronicle-network:
    driver: bridge

volumes:
  chronicle-data:
  chronicle-config:
  chronicle-logs:
